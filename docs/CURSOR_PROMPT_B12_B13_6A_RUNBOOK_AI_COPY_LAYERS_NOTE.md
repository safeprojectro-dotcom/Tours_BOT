# CURSOR_PROMPT_B12_B13_6A_RUNBOOK_AI_COPY_LAYERS_NOTE

Ты продолжаешь проект Tours_BOT.

Cursor mode: Agent.

Это docs-only correction после CURSOR_PROMPT_B12_B13_6_ADMIN_SHOWCASE_PUBLISH_OPS_RUNBOOK.

Не менять app/.
Не менять tests/.
Не менять alembic/.
Не менять mini_app/.
Не менять runtime code.

## Context

B12/B13.6 added docs/ADMIN_SHOWCASE_PUBLISH_RUNBOOK.md and links from CHAT_HANDOFF, OPEN_QUESTIONS, BUSINESS plan.

Important architecture clarification:

Supplier Offer showcase publication should eventually distinguish three layers:

1. Supplier raw input
   - what supplier/admin originally entered
   - may contain rough wording, wrong language, placeholders

2. AI-prepared public copy
   - AI can draft/translate/structure marketing copy
   - AI must not invent facts
   - AI draft must be reviewed/approved by admin
   - not implemented as final publish source yet unless existing approved packaging fields are explicitly wired

3. Final showcase preview
   - exact Telegram post generated by deterministic builder
   - what admin reviews before POST /publish

Key principle:

AI may prepare marketing wording, but facts like date, price, sales_mode, capacity, linked execution truth remain from source-of-truth data. Admin remains final publisher.

## Goal

Update docs/ADMIN_SHOWCASE_PUBLISH_RUNBOOK.md with a short section explaining the three layers:

- Raw supplier input
- AI-prepared public copy
- Final showcase preview

Also update briefly, if appropriate:

- docs/CHAT_HANDOFF.md
- docs/OPEN_QUESTIONS_AND_TECH_DEBT.md
- docs/SUPPLIER_OFFER_TO_TOUR_BUSINESS_PLAN.md

Keep it concise.

## Required wording/policy

Document:

- Admin should check both source/raw facts and final preview.
- In the future, admin should also review AI-prepared public copy before it becomes publish source.
- Current preview shows the final deterministic publish output.
- The system does not auto-rewrite bad raw copy at publish time.
- AI-generated copy is not automatically trusted.
- AI cannot invent or mutate price/date/program/availability.
- Admin approval remains required.

## Non-goals

Do not document as implemented:

- AI final publisher
- automatic AI rewrite at publish time
- mandatory AI copy approval workflow
- DB fields for approved public_ro_copy unless already implemented
- preview_hash enforcement
- admin UI

## Final report

Report:
1. Files changed
2. AI copy layers note added
3. Docs-only confirmation
4. Future work