You are continuing Tours_BOT after B10.3.

Goal:
B10.4 — Full Bus Fixed-Charter Reservation Execution.

Current state:
- Tour #4 is visible in Mini App catalog.
- sales_mode = full_bus.
- B10.3 policy exposes:
  - bookable_as_full_bus_package = true
  - seat_selection_ux = fixed_charter
  - catalog_charter_fixed_seats_count = 56
  - reservation_cta_semantic_key = mini_app_cta_reserve_full_bus
- UI shows “Reserve the bus”, but reservation preparation fails with:
  “tour is not available for reservation preparation”
- My bookings does not show anything because no reservation/order is created.

Business decision:
A fixed full-bus supplier offer is a ready-made product.
Customer may reserve the whole bus/package directly in Mini App.
This is NOT custom_request.
This is NOT per-seat booking.
The customer must not choose arbitrary seats_count.

Goal:
Make full_bus fixed-charter reservation execution work end-to-end:
catalog/detail → preparation → reservation → payment-entry continuation → My bookings.

Core rule:
For bookable full_bus fixed-charter tours:
- seats_count is automatically fixed to catalog_charter_fixed_seats_count / seats_total.
- UI must not ask user to choose seat count.
- Backend must not require arbitrary user seats input.
- Reservation should reserve the whole bus/package.
- Existing Layer A reservation/payment services should be reused.

Files to inspect/change:
- app/services/mini_app_reservation_preparation_service.py
- app/services/mini_app_booking_service.py
- app/services/tour_sales_mode_policy.py
- app/schemas/mini_app.py or relevant mini-app schemas
- app/api/routes/mini_app.py
- mini_app/app.py
- mini_app/api_client.py
- mini_app/ui_strings.py
- tests/unit/test_services_mini_app_reservation_preparation.py
- add new test module if needed

Required backend behavior:
1. Preparation endpoint for full_bus fixed-charter:
   - must allow tour if policy.bookable_as_full_bus_package == true
   - must return fixed seats_count/capacity info
   - must expose that seat selection is fixed
   - must not return error “tour is not available for reservation preparation”
2. Reservation creation for full_bus fixed-charter:
   - if policy.bookable_as_full_bus_package == true:
     seats_count must be forced to catalog_charter_fixed_seats_count or tour.seats_total
   - ignore/override arbitrary client seats_count if needed, or validate it equals fixed count
   - do not allow partial seats for full_bus fixed-charter
3. Per-seat behavior must remain unchanged.
4. Full_bus unavailable/view_only tours must remain blocked.
5. Do not change payment logic.
6. Do not create new payment provider behavior.
7. Do not change supplier offer lifecycle.
8. Do not publish Telegram.
9. Do not touch media pipeline.

Required UI behavior:
1. Tour detail full_bus:
   - price line should not say “56 / 56 seats left”
   - should say whole bus/package price + capacity
2. Preparation screen full_bus:
   - no seat dropdown/free numeric picker
   - show fixed reservation summary:
     “Whole bus reservation”
     “Capacity: 56 seats”
     “Price: 3000 RON total”
   - confirm CTA should be “Reserve the bus”
3. My bookings:
   - once reservation is created, booking should appear
   - copy should show full bus / whole vehicle reservation if available
4. Language:
   - do not solve full multilingual architecture here
   - but avoid losing content when selected language has no translation
   - keep fallback readable

Tests:
Backend:
- full_bus fixed-charter preparation succeeds
- preparation returns fixed seats_count / seat_selection_ux fixed_charter
- reservation create for full_bus uses seats_total automatically
- client cannot reserve 5 seats on full_bus fixed-charter
- per_seat reservation prep unchanged
- full_bus view_only/unavailable still blocked
- no payment/order side effects beyond normal reservation
- created order seats_count == seats_total for fixed full_bus

UI/API tests if present:
- full_bus detail/prep labels use full-bus semantic keys
- no arbitrary seat picker for fixed_charter

Manual smoke target:
- Tour #4
- open details
- click Reserve the bus
- preparation screen loads
- confirm reservation
- My bookings shows new temporary reservation
- payment-entry path remains existing Layer A

Before coding:
1. summarize observed B10.4 bug from screenshots
2. explain fixed full_bus execution rule
3. list expected files
4. state non-goals

After coding:
1. files changed
2. backend preparation behavior
3. reservation behavior
4. UI changes
5. tests run
6. confirm per_seat unchanged
7. confirm no Telegram/payment rewrite/supplier lifecycle/media changes
8. next safe step: B10.4 smoke on Tour #4