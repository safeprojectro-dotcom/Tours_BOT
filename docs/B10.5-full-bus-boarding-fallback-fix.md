You are continuing Tours_BOT after B10.4 smoke.

Goal:
B10.5 — Full Bus Boarding Fallback Fix.

Observed bug:
Fixed full-bus Tour #4 reaches preparation screen, but Preview summary fails:
GET /mini-app/tours/B10-SO8-8b4894/preparation-summary?seats_count=56&boarding_point_id=1&language_code=ro
→ 400 Bad Request
UI shows: invalid reservation preparation selection.

Root cause:
Tour #4 has no real boarding_points.
Mini App UI renders fallback "Departure - As published in the tour", but sends fake boarding_point_id=1.
Backend validates boarding_point_id and rejects it.

Important normal flow reminder:
This must remain behind the safe chain:
Supplier Offer → admin review → approved_for_publish → bridge creates Tour draft → admin activate_for_catalog → Mini App visible.
B10.5 must NOT bypass admin approval, bridge, activation, or publish controls.

Business rule:
For fixed full_bus ready-made offers, boarding may be "as published in the tour" and can be confirmed later.
A missing boarding point must not block whole-bus package reservation if the tour is otherwise bookable_as_full_bus_package.
This does NOT apply to normal per_seat tours.

Required behavior:
1. For bookable full_bus fixed-charter:
   - if no real boarding points exist, preparation may use a virtual/default boarding option.
   - preparation-summary must accept missing/null/virtual boarding selection.
   - reservation creation must work without a real boarding_point_id, using a safe default snapshot/label.
2. UI must not send fake boarding_point_id=1.
   - If fallback option is virtual, send no boarding_point_id or a clear virtual marker if schema supports it.
3. Per-seat tours must still require valid real boarding_point_id when required by existing logic.
4. Full_bus unavailable/view_only must remain blocked.
5. No Telegram/channel publish.
6. No supplier lifecycle changes.
7. No payment provider/order rewrite beyond normal reservation creation.
8. No media work.
9. No recurring offers.

Files to inspect/change:
- app/services/mini_app_reservation_preparation.py
- app/services/mini_app_booking.py
- app/services/reservation_creation.py
- app/schemas/mini_app.py or prepared schemas if relevant
- app/api/routes/mini_app.py
- mini_app/app.py
- mini_app/api_client.py
- mini_app/ui_strings.py
- tests/unit/test_services_mini_app_reservation_preparation.py
- tests/unit/test_services_mini_app_booking.py
- tests/unit/test_api_mini_app.py

Backend implementation expectation:
A. Preparation response:
- When full_bus + bookable_as_full_bus_package + no real boarding points:
  return one virtual/default boarding option with:
  - id: null or a special virtual id only if API can safely handle it
  - label: "Departure - As published in the tour"
  - is_virtual/default marker if schema allows
- If adding schema fields is too broad, keep it minimal and handle absence with UI.

B. Preparation summary:
- If bookable_as_full_bus_package and no real boarding points:
  allow boarding_point_id to be omitted/null.
  use default label/snapshot:
  "Departure - As published in the tour"
- Reject fake non-existing boarding_point_id for normal tours.
- Do not allow arbitrary boarding bypass for per_seat.

C. Reservation creation:
- For full_bus fixed-charter with no real boarding points:
  create order/reservation with default boarding label/snapshot or nullable boarding point according to existing model constraints.
- If Order requires boarding_point_id non-null, create/resolve a system default boarding point attached to the tour; do not use random id=1.
- Keep per_seat unchanged.

D. UI:
- If fixed_charter and boarding option is virtual/default:
  do not send fake id=1.
- Preparation screen should show:
  "Departure - As published in the tour"
  and continue.
- Preview summary should work.
- Confirm reservation should work.

Tests:
1. full_bus fixed-charter without boarding points:
   - preparation succeeds
   - preparation-summary succeeds with no boarding_point_id
   - reservation creation succeeds
   - order seats_count == seats_total
   - order total_amount == base_price
2. fake boarding_point_id is not accepted for per_seat.
3. per_seat with real boarding point unchanged.
4. full_bus fixed-charter with real boarding point still works.
5. full_bus view_only/unavailable remains blocked.
6. no Telegram/publish/supplier lifecycle/media changes.

Manual smoke after deploy:
- Tour #4
- Details → Reserve the bus
- Preparation screen shows no per-seat selector
- Boarding fallback shown as "Departure - As published in the tour"
- Preview summary works
- Confirm reservation works
- My bookings shows temporary reservation
- amount is 3000 RON, not 168000 RON

Before coding:
1. summarize observed 400 from logs
2. explain why this must remain after admin approval/activation only
3. list expected files
4. state non-goals

After coding:
1. files changed
2. backend fallback behavior
3. UI request behavior
4. tests run
5. confirm per_seat unchanged
6. confirm no admin/publish/Telegram/supplier lifecycle/media/recurring changes
7. next safe step: B10.5 smoke on Tour #4